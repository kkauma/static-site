{
  "dependencies": {
    "fs-extra": "^11.2.0",
    "gray-matter": "^4.0.3",
    "marked": "^15.0.6",
    "serve": "^14.2.4"
  },
  "scripts": {
    "build": "node build.js",
    "serve": "serve public -p 4000",
    "dev": "npm run build && npm run serve",
    "clean": "rimraf public/*"
  }
}
